# Prompt 6 — Email Header Analyzer (Part 1)
# Files: 
# - src/app/tools/email-headers/page.tsx (server page with metadata)
# - src/components/tools/EmailHeaderAnalyzer.tsx (client component - PARTIAL)
# Status: PARTIAL — need rest of EmailHeaderAnalyzer component
# Issues:
# - Template literal regex needs backticks fixed: `^${name}:\\s*(.+)$`
# - Need: parseHeaders(), auth parsing, hop extraction, spoofing detection, risk scoring, JSX
